/**
 * Dormant device-level provider auth UI.
 *
 * Re-enable only after the "sign in once, provision every Sprite" lifecycle is
 * defined and tested. Per-Sprite integrations are the supported path for now.
 */
export const ENABLE_GLOBAL_PROVIDER_AUTH = false;
