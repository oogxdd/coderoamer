# The web devDependency (@expo/webpack-config) pins an older Expo than this app
# uses, so npm's peer-dependency resolver errors out. Accept the resolution.
# Applies locally and on EAS cloud builds.
legacy-peer-deps=true
